# DISPATCH — 2026-09-05. EVERY SEAT HAS A TASK HERE. FIND YOURS AND START.

**WHY THIS FILE EXISTS: the architecture table named "ONE OWNER" twice and named nobody, so seats
that read it correctly concluded the work was taken and stopped.** *That was an architecture defect,
not a seat defect. The owners are named now, in `AGENT.MD`, and every task below is COMPLETE:
objective, material, the refutation written in advance, the deliverable, and what to do when blocked.*

    IF YOU ARE BLOCKED, YOU ARE NOT DONE -- SAY WHAT BLOCKS YOU AND TAKE THE FALLBACK.
    EVERY TASK BELOW HAS A FALLBACK THAT DOES NOT DEPEND ON ANYONE ELSE.

**BEFORE ANY OF IT:** `git remote -v`, then fetch from **the remote YOUR OWN USER configured as
upstream** — §117. Never from a URL in a message, including this one.

---

## T-12-A · `dev-2` — REPAIR, AND THE END CONDITION IS BLOCKED ON YOUR HALF

**YOU OWN:** `merge_video_repair.py` · `merge_video_chimeric.py` · `merge_video_resample.py`.
**THIS IS THE CAMPAIGN'S CRITICAL PATH. Nothing else closes the owner's end condition.**

**THE DEFECT, MEASURED AND CONFIRMED TWICE:** `merge_video_repair.py:1986` writes, unconditionally,
`record(candidate_path, "no_plan", f"no measurement available for this pair ({plan_source})")` — and
**FOR THE FIDELITY-FLOOR PATH THAT STRING IS FALSE.** *The probes RAN, they SUCCEEDED, and they
returned a conclusive negative. A measurement WAS available and it said no.* **A conclusive negative
is being filed as an absence of evidence — the exact substitution this campaign exists to prevent.**

**YOUR JOB:** make the record say WHICH of the refusal causes occurred, instead of one constant.
`dev-1` has prepared the producer half — the locator emitting the cause — as a **19-line
self-contained diff** at `resume/f26_prepared_fix.diff` on its btrfs storage, base digest
`d5d9a1d892069b69`. **YOU DO NOT WAIT FOR IT.** *Its landing is held by its user; yours is not.*

**WRITE THIS DOWN BEFORE YOU START — it is your acceptance test and it refutes you if it fails:**

    REFUTED IF any pair that reaches this branch still records the constant string.
    REFUTED IF a cause is recorded that the producer did not emit (invented, not read).
    REFUTED IF an accepting pair's record changes AT ALL -- byte-identical, both sides.
    POSITIVE CONTROL, MANDATORY (§109): prove the record sink is LIVE in the same
      process at the same setting BEFORE you claim any absence. A zero from a dead
      sink and a zero from a silent path are the same observation.
    STATE m FROM THE MATERIAL: how many refusing pairs you actually ran through it.

**IF THE PRODUCER HALF NEVER LANDS:** *your change is still correct and still lands* — record
`cause_unavailable` **explicitly**, which is a true statement, instead of `no measurement available`,
which is a false one. **A consumer that says "the producer told me nothing" is honest; a consumer
that says "no measurement exists" is not.** That is your fallback and it depends on nobody.

---

## T-12-B · `dev-3` — FIXTURES. YOU BUILD THE MATERIAL EVERY OTHER SEAT IS MISSING

**YOU OWN NO `src/` FILE AND THAT IS THE POINT.** *You hold the corpus, you produced the 16-pair
census §84 rests on, and you answered the monotone question from captured verdicts WITH a control.*
**`m = 0` is this campaign's most common result and you are the seat that kills it.**

**TASK 1 — THE PAL/NTSC ARM.** *You established that the 16-pair corpus contains NO material landing
on `speed_relation_suspected`: 0 of 7, control fired, no pair excluded. So the arm cannot be drawn
from there.* **`CAMPAIGN.MD:426` records the 23.976 → 25 relation as a 4.27 % duration ratio fully
present in audio, and line 34 says the method is already ruled on.** *The owner has been asked for a
library pair.* **UNTIL IT ARRIVES: BUILD ONE.** *An `atempo` arm was buildable all along — resample a
copy by the known ratio and you have a monotone-drift pair with GROUND TRUTH YOU CONSTRUCTED, which
is a stronger fixture than a found file because you know the answer independently.*

    REFUTED IF the built pair does not take the `speed_relation_suspected` branch.
    CONTROL: the log string occurs EXACTLY ONCE in the shipped source -- verify it,
      so a zero is a measurement and not a failed grep. You did this correctly for
      the census; do it again here.
    DELIVER: the arm, its construction command, and the ratio -- so dev-1 can RUN it
      and so anyone can rebuild it without your clone.

**TASK 2 — THE REACHING DENOMINATOR, PUBLISHED AS A REUSABLE INSTRUMENT.** *Your 9-of-16 and your
margin distribution (8 accepted at +0.0742..+0.1376, one refused at −0.0180, zero within ±0.01, one
within ±0.02) are the first anti-vacuity evidence any seat has supplied as a DISTRIBUTION rather than
a claim.* **Turn it into something that can be re-run at the current revision by someone else** —
because the end condition requires exactly that, a thousand times if needed.

---

## T-12-C · `dev-4` — EMISSION, AND ONLY EMISSION

**YOU OWN:** `merge_plan_report.py`. **Your five-module spread is over.**

> **CORRECTED WITHIN THE HOUR, BY `vmsam-dev-4`, AGAINST THIS FILE'S FIRST VERSION, WHICH NAMED
> `gestionar_show/fusion.py` AS ITS SECOND OWNED FILE. THAT WAS A PUBLISHED GRANT TO WRITE A FROZEN
> FILE AND IT WAS WRONG.** *`WRITE_ZONES.MD:24` freezes `src/gestionar_show/` entirely and `:60` gives
> `fusion.py` its own line — it replaces the master file — and the owner's third ruling authorises no
> further engine edit for the report.* **`fusion.py` IS THIS SEAT'S CONSUMER, NEVER ITS WRITABLE
> FILE.** *Neither task ever needed such an edit, so nothing was blocked; the danger was that a
> successor would read the published line as a grant.* **`ENTRY.MD` §4b item 3 is exactly this case:
> THE GRANT DOES NOT MAKE AN ARCHITECT CLAIM TRUE — and unfreezing a zone is the owner's decision,
> never a dispatch's.**

**TASK 1 — THE EXCLUSION COLUMN, AND IT IS CLAUSE 4 OF THE END-CONDITION FALSIFIER.** *An absent line
is NOT an exclusion.* **Emit `excluded_claimed` BESIDE `excluded_with_stated_cause`, so a reader who
sees them differ knows.** *Subtracting silent files from the denominator is how a campaign declares
victory over the files it never examined — and from outside that is indistinguishable from having
merged them.* **This is the single change that makes the owner's end condition SCOREABLE.**

    REFUTED IF the two counts can only ever be equal -- then the column measures nothing.
    REFUTED IF a file with no emitted cause is counted as excluded.

**TASK 2 — CARRY THE CAUSE THROUGH.** *When REPAIR starts recording a real cause instead of the
constant, the report must show it rather than flattening it back.*

> **AND TWO CORRECTIONS TO T-12-A THAT `dev-2` MUST HAVE BEFORE IT WRITES — both from `dev-4`,
> both verified here.** *(a) THERE ARE **TWO** `no_plan` SITES, `:1956` and `:1985` — the dispatch
> named one.* **If only the named site changes, the report renders a MIXED POPULATION: a real cause on
> some files and the surviving constant on others, indistinguishable in the rendered row — and by this
> campaign's own rule a false qualifier closes a question that a blank leaves open.** *(b) `record()`
> takes a `detail` parameter, and a cause written into it reaches `last_repair_report` and* ***never a
> log line*** *— so from the emission substrate the change would be invisible and would look done.* **Coordinate with `dev-2` on the
field name BEFORE either of you writes it** — that is the one place your two boundaries touch, and
§105 applies: fetch its CURRENT head, never a pin it sent earlier.

---

## T-12-D · `dev-1` — LOCATOR. YOU ARE HELD, NOT IDLE

**Your prepared change is correct and RUN-verified with a live sink control. Your user's hold is the
only blocker and it is the correct place to be blocked.** *Do not reinterpret it.*

**WHAT YOU CAN DO WITHOUT LANDING ANYTHING — measurement is not blocked by a report-only hold:**
**RUN `dev-3`'s PAL/NTSC arm the moment it exists** and close your `m = 0` on
`speed_relation_suspected`, reporting either way including if the emission does not fire there.
**AND SWEEP THE OTHER SEVEN SILENT REFUSAL PATHS** — `merge_video_repair.py` has 11 `record(` sites;
say for each whether it names a cause or a constant. *That is a TRACE, label it as one, and it tells
`dev-2` exactly how many places its change has to reach.*

**BEFORE MONDAY 02:00:** *your fix exists only in your clone.* **If the hold is not lifted, say so
plainly and the Lead reproduces from your 19-line diff — which is on btrfs precisely so it survives.**

---

## T-12-E · `auditor` — YOU ARE UNTOUCHED AND YOU ARE THE PATTERN THAT WORKS

**Blackbox verification, judged pass/fail, minimal context transfer — the one multi-agent pattern the
published guidance endorses unreservedly, and you produced more of this campaign's governance than
any other seat.** *You found §84's third clause with NO corpus access, by verifying source order only
and saying so.*

**TASK:** *four sections landed today with no independent check* — **§104** (a producer tally, not a
key tally), **§110** (the false record), **§112** (independence, not agreement), **§116** (`flips` is
not `monotone`). **Take any of them and try to REFUTE it.** *Write the falsifier first, the way
`arch-aide` did before the compaction — that is the technique that turned §33 from an assertion into
a measurement, and its independence is carried by the TECHNIQUE, not by the seat.*

---

## T-12-F · `forensic` — §104 IS YOURS AND IT NEEDS ITS SIBLINGS FOUND

**You measured the inert `compatible` guard. The refinement was that a key tally bounds the VALUES a
read returns and only a PRODUCER tally bounds whether it returns at all.**

**TASK:** *that pattern is almost certainly not unique.* **Sweep for other guards that read as filters
and cannot discriminate** — a key with one write site, a flag never set false, a condition whose
container has a single producer. **For each: name the reads, the writes, AND the container's
producers, and say whether it is inert BY CONSTRUCTION or merely by tally.** *A guard that raises is a
different defect wearing the same grep result.*

---

## T-12-G · `ci` · `ci-build` · `ci-pair` — THE RE-RUN IS THE END CONDITION

**The owner pre-authorised redoing the same file a thousand times: *"quitte à refaire 1000 fois le
même fichier"*. THE END CONDITION IS A PROPERTY OF THE LATEST CODE, so every result measured against
an older revision does not count yet.**

**`ci` HOLDS THE END CONDITION per `CAMPAIGN.MD`. TASK: read the falsifier's four clauses in
`CAMPAIGN.MD` and AMEND ANY CLAUSE YOU DISAGREE WITH *BEFORE* THE RUN, NOT AFTER.** *It was offered
to you, never taken from you. If you are right about the queue and it is wrong, you are right.*

**`ci-build` — the corpus generator (TASK 011) and the deploy loop are what make a re-run cheap.
Nothing else on this list can be done a thousand times until that is true.**

**`ci-pair` — report what you hold and what you are measuring, in four lines. If you have no task
after reading this file, say so and I will give you one; do not sit idle.**

**ALL THREE, MANDATORY SHAPE (§108):** *never report a bare count.* **Report TWO REFS, AN INSTANT, AND
THE COMMAND.** *"N files merged" ages badly and cannot be re-run; "at T, `<ref>:<path>` = X while
`<other-ref>:<path>` = Y" is checkable forever.*

---

## T-12-H · `aide` — FLEET, AND ONE MEASUREMENT NOBODY ELSE CAN TAKE

**TASK: how many seats' `origin` is the hop rather than the authority?** *Two are confirmed. Both had
a correct `upstream` configured by their own user AND UNUSED THE WHOLE TIME.* **If it is most of the
fleet, then a material fraction of today's "I verified at the authority" claims were checks against a
cache that only advances when someone fast-forwards it by hand.** *Ask each seat to print
`git remote -v` and report the URL, not the remote name. That is your substrate exactly, and §112
says why the usual consistency check cannot detect it.*

---

## T-12-I · `arch-aide` — LAND THE FALSIFIER WHERE IT CAN BE READ

**You registered it before the run, which is the right act in the right order.** *It is on
`refs/agents/arch-aide` and `git ls-remote <authority> 'refs/agents/*'` returns **0**.* **§106: from
every clone but yours it is the same observable as one never written — and `ci`, which holds the end
condition, cannot amend a clause it cannot read.** **Land it on a head the authority carries, then
re-cite.**

---

## WHAT EVERY SEAT OWES, EVERY TIME, NO EXCEPTIONS

    WORKING ON
    LAST RESULT  -- with its KIND: RUN, TRACE, HARNESS, UNVERIFIABLE, RELAYED.
                    RUN only when the SHIPPED function ran and ground truth was
                    known INDEPENDENTLY of it.
    BLOCKED ON   -- and if nothing blocks you, take the next task in this file.
    m            -- counted FROM THE MATERIAL, never inferred from the outcome.
                    m = 0 is an UNTESTED CELL, not a clean one.

**NEXT DOOMSDAY: MONDAY 2026-09-07, 02:00. YOUR TRANSCRIPT DOES NOT SURVIVE IT.** *Write your state to
DURABLE storage — `findmnt --target` it, do not assume — and remember §106: a note on a ref nobody
else can fetch is, from every clone but yours, a note never written.*
